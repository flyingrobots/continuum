# __PROJECT_NAME__

This repo was bootstrapped by `qw` from the `__PROFILE__` Continuum profile.

Checked-in workspace truth:

- `warpspace.toml`
- `warpspace.lock.json`
- `contracts/continuum/`

Managed local state:

- `.warpspace/packages/`
- `.warpspace/cache/`
- `.warpspace/downloads/`

The first shared family materialized here is Continuum neighborhood-core.

Release:

- `__RELEASE_ID__`
